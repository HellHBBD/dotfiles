---
description: Create one Git worktree, branch, and initialized OpenCode session for each task.
agent: worktree-queue
subtask: false
---

Create Git worktrees, branches, and initialized OpenCode sessions from this
ordered task input:

$ARGUMENTS

Perform only the initialization workflow defined by the `worktree-queue` agent.
Do not plan, implement, review, verify, commit, merge, push, dispatch
subagents, or modify project files.
