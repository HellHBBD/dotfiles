-- Minimal Hyprland configuration.
--
-- This layer contains only the configuration required to keep the
-- Hyprland session usable when optional desktop components fail.
--
-- Do not add appearance, widgets, plugins or application-specific
-- rules here.

require("hyprland.env")
require("hyprland.monitors")
require("hyprland.input")
require("hyprland.safety")
